import flet as ft

def recent_activities_card():
    return ft.Card(
            content=ft.Container(
                padding=10,
                alignment=ft.alignment.center,
            ),
            color=ft.colors.WHITE38,
        )